# :milky_way: HappyCode eslint-config

## Установить конфиг
```sh
yarn add -D @happycode-core/eslint-config
```

## Как использовать
Создать файл `eslint.config.js` и добавить в него содержимое:

```js
import { eslint } from './index';

export default eslint();
```
